# 2026-09-12

## Session 1 — Brain initialised

### What was done

- Created `project-brain/` from the skill templates and seeded every file from the
  repo's own `context.md`, which had been serving as an informal brain, plus
  `CLAUDE.md`, `README.md` and the commit history.
- Recorded the three places the live code has moved past `CLAUDE.md`: Atlas is the
  system of record rather than a Phase 2 backup, Salesforce is switched off rather
  than primary, and auth is Google **plus** credentials rather than Google-only.
- Carried all eleven hard-won gotchas across into `01-architecture.md` verbatim in
  substance, and reconstructed six decisions with their rejected alternatives into
  `02-decisions.md`.
- Appended the `## Project Brain` section to `CLAUDE.md` (append only — nothing else
  in that file was touched).
- Registered the brain in the Obsidian vault as a junction.

### Problems hit & how fixed

- **The Obsidian vault path in the skill is off by one level.** The skill documents
  `Documents\Lakshya's Thinking\`, but that folder only contains another folder of the
  same name, and the inner one is the real vault (it holds `.obsidian`). A junction in
  the outer folder would never appear in Obsidian. The junction was created at
  `Documents\Lakshya's Thinking\Lakshya's Thinking\snapcard`.
- **`context.md` overlapped this brain almost completely.** Not resolved unilaterally
  — deleting it is a refactor nobody had asked for. Raised with the user as a question
  instead; they chose to delete it outright rather than leave a pointer file. Its
  content is in the brain and the original is `git show 784b16d:context.md`.
- **`context.md` says `production` is 19 commits behind `main`; it is actually 30.**
  The count had already rotted in the weeks since it was written, so the brain records
  the rule instead: `production` is well behind `main`, and merging **is** the deploy.
- **A bash heredoc failed writing `01-architecture.md`** (`unexpected EOF while looking
  for matching quote`) — the markdown's backticks and apostrophes did not survive the
  shell. Rewritten with the Write tool.

### Files touched

- `project-brain/00-overview.md`, `01-architecture.md`, `02-decisions.md`,
  `03-progress.md`, `04-tasks.md`, `journal/2026-09-12.md`
- `CLAUDE.md` (appended one section)
- `context.md` (deleted)

### Repo state at init

`salesforce-connect` @ `d0cacc8`, level with `origin/main` and `origin/salesforce-connect`,
working tree clean. `origin/production` is 30 commits behind. No source code was
changed this session.

## Session 2 — Planning docs folded in

### What was done

- Fixed the Obsidian vault path in `~/.claude/skills/brain/SKILL.md` (init step 7 and
  the vault section) so future `/brain init` runs in other repos land in the inner
  `Lakshya's Thinking`, not the wrapper. The existing snapcard junction was already
  correct and was left alone.
- Read `PLAN-1-salesforce.md`, `PLAN-2-atlas.md`, `SETUP.md` and `README.md` and
  distributed them: contracts, limits, the Atlas shape and the sync-claim protocol into
  `01-architecture.md`; eight decisions into `02-decisions.md`; event-day readiness and
  the unreachable-by-design endpoints into `00-overview.md`; the leftover work into
  `04-tasks.md`, bucketed by whether it can actually be done today.
- Verified every number against the code before writing it down, which caught four
  places where the plans no longer describe the app.

### Problems hit & how fixed

- **The plans disagree with the code in four places.** Recorded both sides in
  `02-decisions.md` rather than overwriting: per-rep attribution (`PLAN-1` Task 12 says
  it is impossible and gives a reason that was true for a Salesforce-only world;
  `2f5f58d` made it real via Atlas `capturedBy`), the `0.0.0.0/0` network rule, the
  Dexie-backed `/leads`, and consent.
- **`SETUP.md` will misconfigure the next environment.** §3 and §4 both name the
  database `snapcard`; it is `snapcard1`. Follow §4 as written and you get a working
  Atlas user scoped to the wrong database. Logged as a task, not fixed — the user asked
  for no source file to be touched.
- **`PLAN-2` Task 1 specifies `serverSelectionTimeoutMS: 3000`; the code uses 30s.**
  Not a contradiction to log as a conflict — the plan was superseded by a measurement
  (handshakes on this link can take 21s). Recorded as the reason the number moved.
- **"Never built" is not the same as "outstanding".** `/api/reconcile` and
  `/api/leads/backup` are both unreachable while `SALESFORCE_ENABLED=false` — the
  backup retry only fires when Salesforce *succeeds* and Atlas fails. Filing them under
  Now would have handed the next session untestable work; they sit under an explicit
  "blocked on Salesforce coming back" heading instead.
- **Did not invent rationales.** The upsert-not-POST rule and the Internal OAuth
  consent screen have no stated why anywhere in the docs. Both entries say so rather
  than reconstructing a plausible one.

### Files touched

- `~/.claude/skills/brain/SKILL.md` (outside the repo, not versioned here)
- `project-brain/00-overview.md`, `01-architecture.md`, `02-decisions.md`,
  `04-tasks.md`, `journal/2026-09-12.md`

### Next time start here

Add the Credentials provider to `src/lib/auth.ts` — alongside the Google provider, not
replacing it, and leaving the thinkvibes.com domain restriction exactly as it is. Then
the password form on `src/app/(auth)/login/page.tsx`. `src/lib/password.ts` and
`src/lib/users.ts` are already written and tested; nothing wires them up yet.
